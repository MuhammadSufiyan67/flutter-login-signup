

void main()
{
  String myName = "Muhammad Suifyan";        // Initialize a string
  print(myName);
  print(myName.length);                      // Length function uses to describe the length of a String
  print(myName[2]);                          // Index uses to describe the elements of string
 print(myName.toUpperCase());              // Uppercase uses for showing all elements of a string in Uppercase order
  print(myName.toLowerCase());               // Lowercase uses for showing all elements of a string in Lowercase order
  print(myName.split(''));                   // Split function uses to split the complete string in one by one order
  print(myName.replaceAll(myName, 'abc'));    // This function uses for replace the current string with new one
  print(myName.contains('S'));                 // This function checks is S contain in String or not

}